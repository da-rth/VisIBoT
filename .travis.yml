matrix:
  include:
    - language: python
      python:
      - - "3.8"
      before_script:
        - cd src/processing
        - pip install -r requirements.txt
      script:
        - python -m unittest discover
        - python -m pylama
    - language: node_js
      before_script:
        - cd src
        - npm install
      script:
        - npm run eslint